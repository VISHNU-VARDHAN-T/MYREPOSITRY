#include<stdio.h>
int main()
{
float distance,time,speed;
printf("Enter the distance:");
scanf("%f",&distance);
printf("Ente the time:");
scanf("%f",&time);
speed=distance/time;
printf("The speed of the vehical is:%f\n",speed);
}
